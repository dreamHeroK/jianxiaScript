--NPC默认对话脚本
Include("\\script\\online_activites\\head\\activity_kill_npc_task.lua")

function main(bTag)
	--活动
--	local tID = {125, 140}
--	for i = 1, getn(tID) do
--		if g_akct_ShowDialog(tID[i], bTag) then
--			return
--		end
--	end
	
--	local tbSay = {};
--	Say("<color=green>Th玭 trng: <color>N╩ nay l? n╩ i thu!",getn(tbSay),tbSay);
end